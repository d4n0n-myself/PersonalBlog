@using Westwind.AspNetCore.Markdown;
@model PersonalBlog.Web.Pages.ShowPosts

<div class="list-group"> 
    @foreach (var post in Model.List)
    {
        @Markdown.ParseHtmlString(post.Body)<br/>    
    } 
</div>